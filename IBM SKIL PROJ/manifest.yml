applications:
  - name: kisan-mitra
    memory: 512M
    disk_quota: 1G
    instances: 1
    buildpacks:
      - python_buildpack
    command: gunicorn app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120
    env:
      FLASK_ENV: production
      FLASK_DEBUG: "false"
